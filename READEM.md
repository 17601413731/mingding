# 命定

命定是 Windows 下的卡牌大师辅助工具，提供自由选牌和自由移动两项功能。

## 功能

- **自由选牌：**蓝牌、黄牌、红牌各有独立按键。按对应键后，程序按 `W` 开始轮牌，识别到目标牌时再次按 `W` 锁定。
- **自由移动：**按住鼠标右键时，程序按设定间隔重复发送配置的移动键，减少长时间反复点击鼠标造成的手疼。
- 选牌键和功能启停键支持键盘键、鼠标左键、中键及侧键；鼠标右键留给自由移动。设置会自动保存。

## 下载

前往 [GitHub 最新版本](https://github.com/17601413731/mingding/releases/latest)，下载 `mingding-win64.zip`。压缩包适用于 Windows 64 位，无需安装 Python。

## 安装

将压缩包解压到可写入的文件夹，保留整个 `mingding` 文件夹。**右键 `mingding.exe`，选择“以管理员身份运行”**；热键监听和游戏内模拟按键需要管理员权限。不要只复制 exe 文件。

## 使用

1. 在游戏中将卡牌大师的选牌技能键设为 `W`。默认按 `F2` 开启自由选牌；蓝牌按 `F6`、黄牌按 `E`、红牌按 `F7`。点击界面上的对应按键框，可以为每张牌重新录入键盘或鼠标按键。
2. 使用自由移动前，先在游戏内把“鼠标移动按键”设为界面显示的移动键（默认 `N`）。按 `F3` 开启自由移动，再按住鼠标右键使用；重复间隔可在界面调整。
3. 点击“启停键”旁的键帽可修改 `F2`、`F3`。录入时按 `Esc` 取消。关闭窗口后程序仍在系统托盘运行，可从托盘重新打开或退出。

当前屏幕识别区域按 1920×1080 分辨率标定。
