## v1.1.1

- 程序启动时自动请求管理员权限，无需再右键选择“以管理员身份运行”。
- 新安装的默认按键改为蓝牌鼠标侧键 2、黄牌 `E`、红牌鼠标侧键 1。
- 设置现在保存在 `%APPDATA%\mingding\config.json`，更新或移动程序不会覆盖；首次启动自动迁移旧版 exe 同目录的配置。

下载 `mingding-win64.zip` 并解压整个文件夹。双击 `mingding.exe` 后，在 Windows 管理员权限提示中选择“是”。
