## v1.1.0

- 蓝、黄、红牌现在各有独立的选牌按键，识别并锁定对应牌色。
- 选牌键和功能启停键支持鼠标左键、中键、侧键 1/2，也可继续使用键盘键；鼠标右键保留给自由移动。
- 自由移动可配置游戏内移动键和重复间隔，界面说明了游戏中需要设置的按键。
- 增加使用文档，明确下载安装步骤及管理员身份运行要求。

下载 `mingding-win64.zip`，解压后保留整个 `mingding` 文件夹，右键 `mingding.exe` 选择“以管理员身份运行”。
